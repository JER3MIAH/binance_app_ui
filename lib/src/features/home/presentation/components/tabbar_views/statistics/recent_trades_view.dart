import 'package:flutter/material.dart';

class RecentTradeView extends StatelessWidget {
  const RecentTradeView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}